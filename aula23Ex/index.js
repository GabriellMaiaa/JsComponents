/** Escreva uma função que receba 2 numeros e
 * retorne o maior etre eles.*/ 
function max (x, y) {
   return x > y ? x : y;
}
const max2 = (x, y) => { x > y ? x : y};

/** if(x > y) {
        return x;
    }
    else {
        return y;
    }
 */
console.log(max(102, 34))